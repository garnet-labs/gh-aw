---
"gh-aw": patch
---

Upgrade `gh-aw-mcpg` to `v0.2.13`, updating the default MCP gateway image version and recompiling workflow lock files. This release adds OpenTelemetry tracing support and a configurable session timeout via `MCP_GATEWAY_SESSION_TIMEOUT`.
